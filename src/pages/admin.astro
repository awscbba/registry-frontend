---
// SECURITY: Admin access temporarily disabled pending proper role-based access control
---

<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Acceso Restringido - AWS User Group Cochabamba</title>
    <style>
        body {
            font-family: 'Open Sans', sans-serif;
            background: #f7fafc;
            display: flex;
            justify-content: center;
            align-items: center;
            min-height: 100vh;
            margin: 0;
        }
        .access-denied {
            background: white;
            padding: 3rem;
            border-radius: 12px;
            box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
            text-align: center;
            max-width: 500px;
        }
        .icon {
            font-size: 4rem;
            margin-bottom: 1rem;
        }
        h1 {
            color: #e53e3e;
            margin-bottom: 1rem;
        }
        p {
            color: #4a5568;
            line-height: 1.6;
            margin-bottom: 2rem;
        }
        .back-button {
            background: #4299e1;
            color: white;
            padding: 0.75rem 1.5rem;
            border: none;
            border-radius: 6px;
            text-decoration: none;
            display: inline-block;
            font-weight: 500;
            transition: background 0.2s;
        }
        .back-button:hover {
            background: #3182ce;
        }
    </style>
</head>
<body>
    <div class="access-denied">
        <div class="icon">🔒</div>
        <h1>Acceso Restringido</h1>
        <p>
            El panel de administración está temporalmente deshabilitado por motivos de seguridad 
            mientras implementamos controles de acceso basados en roles.
        </p>
        <p>
            Si necesitas acceso administrativo, por favor contacta al administrador del sistema.
        </p>
        <a href="/" class="back-button">← Volver al Inicio</a>
    </div>
</body>
</html>
